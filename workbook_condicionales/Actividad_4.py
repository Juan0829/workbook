#suma de valores en una lista con for
lista_numeros=[2, 4, 7, 10, 1, 8]
suma=0
for numero in lista_numeros:
    suma = suma + numero
print(f"el resultado total de la suma de los valores {lista_numeros} es {suma}")